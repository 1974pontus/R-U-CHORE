import React, { CSSProperties } from 'react';


function Header() {
  return (

    <div style={header}>
 
    </div>
  );
};

export default Header;

const header: CSSProperties = {
    width: '100%',
    height: '15rem',
    background: 'pink',
}